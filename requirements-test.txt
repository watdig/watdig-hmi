# Isolated regression-test environment; production requirements are unchanged.
Flask==3.1.3
Flask-Cors==6.0.5
# Match the pymodbus release recorded in the repository's existing virtualenv.
pymodbus==3.7.2
pyserial==3.5
SQLAlchemy>=2.0,<3
tabulate>=0.9,<1
numpy>=1.26,<3
pytest>=8,<10
