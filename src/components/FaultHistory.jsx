import React, { useEffect, useState } from "react";
import axios from "axios";

const FaultHistory = () => {
  return (
    <div>
      <div>
        <h2>Fault History</h2>
        {/* Fault history component: This will become a dynamic database that reads the data form the SQLITE database */}
      </div>
    </div>
  );
};

export default FaultHistory;
